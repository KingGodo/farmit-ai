spring:
  datasource:
    url: jdbc:h2:mem:farmitai;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    driver-class-name: org.h2.Driver
    username: sa
    password:
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

farmit:
  jwt:
    secret: test-secret-must-be-at-least-32-chars
  otp:
    log-code: false
  admin:
    email: admin@farmit.co.zw
    password: changeme
    phone: +263700000000
